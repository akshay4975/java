class Example
{
	public static void main(String[]args)
        {
	 double subTotal= 10;
	 double tipRate=subTotal*0.15;
	 double total=subTotal+tipRate;
	 
 	 System.out.printf("the tip is %f and total is %f",tipRate,total);
	}
}
	 
	 